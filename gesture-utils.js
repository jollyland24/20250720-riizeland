// gesture-utils.js
export class GestureUtils {
    static detectAdvancedGestures(landmarks) {
        // More complex gesture detection logic
        // Return gesture name and confidence
    }
    
    static calculateHandDistance(landmarks1, landmarks2) {
        // Calculate distance between two hands
    }
    
    static getFingerPositions(landmarks) {
        // Extract finger tip positions
    }
}